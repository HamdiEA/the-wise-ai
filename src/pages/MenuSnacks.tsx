import SnacksMenu from "@/components/menu/SnacksMenu";
import MenuLayout from "@/components/MenuLayout";

const MenuSnacks = () => {
  return (
    <MenuLayout>
      <SnacksMenu />
    </MenuLayout>
  );
};

export default MenuSnacks;